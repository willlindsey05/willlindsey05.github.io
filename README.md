# willlindsey05.github.io

My personal website, built with plain HTML and CSS. No frameworks, no build tools, no dependencies.

## Local Development

```bash
python3 -m http.server 8080
```

Then open `http://localhost:8080`.
